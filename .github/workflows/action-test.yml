name: Matrix Array
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]

jobs:
  Action-Test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v3

      - name: Show apps
        uses: ./.github/actions/action-matrix
        with:
          app: "['app1', 'app2', 'app3']"
          config: "['config1', 'config2', 'config3']"
          
